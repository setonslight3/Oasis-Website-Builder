[build]
  command = "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/synapse run build"
  publish = "artifacts/synapse/dist/public"

[build.environment]
  NETLIFY_USE_PNPM = "true"
  PNPM_VERSION = "10"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
