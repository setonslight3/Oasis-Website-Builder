[build]
  command = "pnpm install && pnpm --filter @workspace/oasis run build"
  publish = "artifacts/oasis/dist/public"

[build.environment]
  NETLIFY_USE_PNPM = "true"
  PNPM_VERSION = "9"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
