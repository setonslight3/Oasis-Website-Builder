[build]
  command = "pnpm install && pnpm --filter @workspace/synapse run build"
  publish = "artifacts/synapse/dist/public"

[build.environment]
  NETLIFY_USE_PNPM = "true"
  PNPM_VERSION = "9"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
